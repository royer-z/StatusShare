<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" android:id="@+id/contactsContactLL">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_margin="15dp" android:id="@+id/contactsContactContainerLL">

        <ImageView
                android:id="@+id/contactsContactProfileImage"
                android:src="@drawable/default_user"
                android:layout_height="96dp"
                android:layout_width="96dp"
        />

        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:layout_margin="5dp" android:id="@+id/contactsContactInfoLL">
            <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:id="@+id/contactsContactName"
                    android:text="@string/contactsContactName"
                    android:textStyle="bold"
                    android:textSize="24sp" android:layout_weight="1"/>
            <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/contactsContactStatus"
                    android:id="@+id/contactsContactStatus"
                    android:textSize="24sp" android:layout_weight="1"/>
        </LinearLayout>

    </LinearLayout>

</LinearLayout>