<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.statusshare">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity android:name=".EditEventActivity"></activity>
        <activity android:name=".AddEventActivity"></activity>
        <activity android:name=".whatever"></activity>
        <activity android:name=".AddContactActivity"></activity>
        <activity android:name=".EditNotificationActivity"></activity>
        <activity android:name=".CreateNotificationActivity"></activity>
        <activity android:name=".SharedProfileActivity"></activity>

        <meta-data
                android:name="com.google.android.gms.ads.AD_MANAGER_APP"
                android:value="true" />

        <activity android:name=".Registration"></activity>
        <activity android:name=".EditProfileActivity"></activity>
        <activity android:name="BottomNavigationActivity"></activity>

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>